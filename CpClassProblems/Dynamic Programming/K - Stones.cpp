#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n, k;
    cin>>n>>k;
    vector<int> v(n);
    for(auto &x:v) cin>>x;
    vector<vector<int>> dp(n, vector<int>(k));
    return 0;
}